(ns landscape.insruction)

(defn find-well [])
(defn INSTUCTION
  [
   {:text "Welcome to our game!" :pos #({:x 100 :y 100}) :up (fn[_])}
   {:text "Some wells are good"  :pos #({:x 100 :y 100}) :up (fn[_])}
  ])
